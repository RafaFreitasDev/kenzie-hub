import styled from "styled-components";
export const StyledRegisterSelect = styled.span`
  label {
    color: var(--color-gray-0);
    font-size: 0.7rem;
    margin: 10px 0;
  }

  select {
    width: 100%;
    background-color: var(--color-gray-2);
    color: rgba(134, 142, 150, 1);
    border-radius: 4px;
    height: 40px;
    padding: 0 10px;
  }
`;
